package foro.hub.Foro_Hub.Security;

/**
 * Record de registro que encapsula un token JWT.
 *
 * Este record se utiliza para representar un token JWT como un objeto inmutable.
 */
public record DatosJWTToken(String jwTtoken) {
}
